[package]
name = "yttt-agent-core"
version.workspace = true
edition.workspace = true
description = "Provider-neutral agent lifecycle and activity state machine"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
uuid = { version = "1", features = ["v4", "serde"] }
